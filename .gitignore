# ── 凭据：绝不入库 ──────────────────────────────────────────
# 真实登录态（内含支付宝 / 学校站点的 Cookie 值）
data/cookies.json
# 线上服务的 admin_token
web/config.json

# ── 本机专属：不入库 ────────────────────────────────────────
# 本地 CLI 的配置。它是「这台机器」的状态而不是代码：
# client_id 是首次运行时生成的随机机器标识，换个环境本来就该重新生成
# （monitor.py 发现为空会自动补一个）。
# 新环境从 config.example.json 复制一份即可。
config.json

# ── 运行期数据：属于「状态」不属于「代码」 ──────────────────
data/history.csv
data/last_state.json
data/room.json
data/run.log
web/data/

# ── 浏览器 profile：几百 MB 的二进制缓存，且含登录态 ────────
.edge-profile/
_sp/
**/profiles/

# ── Python ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 编辑器 / 系统 ──────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini
*.swp

# ── 临时验证产物 ───────────────────────────────────────────
_verify/
_*.js
_*.html

# 注意：docs/ 由 sync-docs.sh 从 dist/ 生成，虽然算「产物」，
# 但 GitHub Pages 要直接读它，所以必须入库 —— 不要忽略它。
